{
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "packageManager": "yarn@1.22.22",
  "devDependencies": {
    "@iconify-json/lucide": "^1.2.68",
    "turbo": "^2.5.4"
  }
}
