{
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*" 
  ],
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "packageManager": "yarn@1.22.22",
  "dependencies": {},
  "devDependencies": {
    "turbo": "^2.5.4" 
  }
}
