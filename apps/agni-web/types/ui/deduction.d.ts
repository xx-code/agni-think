export type EditDeduction = {
    title: string
    description: string
    base: string
    mode: string
}

export type DeductionType = {
    id: string
    title: string
    description: string
    base: string
    mode: string
}
